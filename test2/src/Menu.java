import java.util.Scanner;

public class Menu {
    public static void createMenu () {
        Scanner scanner = new Scanner(System.in);
        String [] menu = {"exit", "create new object", "show object"};
        int i = scanner.nextInt();
        if (i == 1)
        {метод по созданию нового объекта}
        else if (i == 2) {
        показывать инфу из файла (метод)
                    если пусто то кидать ошибку;
        else if (i==0) {
            break;
            }

    }
}
